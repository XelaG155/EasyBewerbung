# Docker Environment Configuration for EasyBewerbung
# Copy this to .env and fill in your values before running docker compose

# PostgreSQL password (change in production!)
POSTGRES_PASSWORD=your-secure-password-here

# Redis URL (for Celery task queue)
# Default works for Docker Compose setup
REDIS_URL=redis://redis:6379/0

# Secret key for JWT tokens - CHANGE THIS IN PRODUCTION
# Generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-change-me

# OpenAI API Key (required for document generation with OpenAI models)
OPENAI_API_KEY=sk-your-openai-api-key

# Anthropic API Key (required for document generation with Claude models)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# Google API Key (required for document generation with Gemini models)
GOOGLE_API_KEY=your-google-api-key

# Google OAuth Configuration (optional, for Google Sign-In)
# Get your Client ID from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=

# CORS Origins (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3001,https://app.easybewerbung.ch
